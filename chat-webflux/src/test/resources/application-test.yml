spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb_${random.uuid}?;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql